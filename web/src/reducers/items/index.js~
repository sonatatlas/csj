import { combineReducers } from 'redux'
import { itemType } from '../../actions/items'


const ItemType = (state=false, action)=>{
    switch (action.type){
    case TOGGLE:
	return action.reddit
    default:
	return state
    }
}

const navbarReducer = combineReducers({
    ItemType
})

export default navbarReducer
